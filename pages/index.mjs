import UserCard from "@/components/UserCard";

// ...existing code...

const HomePage = () => {
  return (
    <div>
      {/* Example usage of UserCard component */}
      <UserCard uid="some-uid" />
    </div>
  );
};

export default HomePage;
